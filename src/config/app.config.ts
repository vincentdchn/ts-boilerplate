export const config = {
  logger: {
    /**
     * The keyword to use when redacting the logger
     * It is used to hide the sensitive information from the logs
     */
    redactedKeyword: "[redacted]",
  },
};
